{
  "name": "formily-request-monorepo",
  "version": "0.2.3",
  "description": "A flexible, lightweight, non-intrusive plugin for extending formily schema request data.",
  "author": "Long Chan",
  "license": "MIT",
  "homepage": "https://github.com/007sair/formily-request",
  "scripts": {
    "build:lib": "pnpm run --parallel -F formily-request build",
    "build:docs": "pnpm run --parallel -F formily-request docs",
    "build:examples": "pnpm -F examples run build-storybook",
    "storybook": "pnpm run build:lib --watch & pnpm run --parallel -F examples storybook",
    "build": "pnpm run build:lib && pnpm run build:docs && pnpm run build:examples"
  },
  "keywords": [
    "formily",
    "schema",
    "request",
    "fetch",
    "dynamic"
  ],
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@formily/antd-v5": "^1.1.2",
    "@formily/core": "^2.2.27",
    "@formily/react": "^2.2.27",
    "@formily/reactive": "^2.2.27"
  }
}
